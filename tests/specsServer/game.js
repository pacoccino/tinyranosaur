var expect = require("chai").expect;

var Game = require("../../server/modules/game");

//var io = require("socketio-mock");

describe('Game', function() {
    it('should listen', function() {
       // Game.listen(io);
    });

});
